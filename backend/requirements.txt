fastapi>=0.115.0
uvicorn[standard]>=0.30.6
pydantic>=2.10.0
python-dateutil>=2.9.0
python-docx>=1.1.0
# Renders the Financial Analysis charts to PNG images for the Word/PDF reports.
matplotlib>=3.7.0
# Optional — PDF rendering. Word generation works without it; the PDF route
# falls back to a styled HTML report if WeasyPrint's native libs are missing.
weasyprint>=60.0
